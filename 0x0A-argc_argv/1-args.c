#include <stdio.h>

/**
 * main - main function, prints number of args in passe to the func
 * @argc: number of parmas
 * @argv: array of params
 * Return: returns 0 to exit code
 */

int main(int argc, char __attribute__((__unused__)) *argv[])
{
	printf("%d\n", argc);
	return (0);
}
