#include <stdio.h>

/**
 * main - entry point of all c code
 *
 *Return: return 0 to exit code
 */

int main(void)
{
	int i;

	for (i = '0'; i <= '9'; i++)
		putchar(i);
	putchar('\n');
	return (0);
}
