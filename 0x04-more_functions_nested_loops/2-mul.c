#include <stdio.h>
#include "main.h"

/**
 * mul - function to multiply two given numbers
 * @a: first param
 * @b: second param
 *
 * Return: return 0 to exit code
 */

int mul(int a, int b)
{
	int res;

	res = a * b;
	return (res);
}
