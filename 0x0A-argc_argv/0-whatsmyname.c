#include <stdio.h>

/**
 * main - entry point for all c code
 * @argc: parameter 1
 * @argv: parameter 2
 * Return:  exit from code with 0
 */

int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
